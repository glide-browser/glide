#!/usr/bin/env node --no-warnings --experimental-strip-types

// run typescript files with node.js, presumes v23+

// @ts-check

import { $ } from "../zx.mts";

$.disable_command_logging();

const args = process.argv.slice(2);
await $`node --no-warnings --experimental-strip-types ${args}`.catch($.handle_error);
