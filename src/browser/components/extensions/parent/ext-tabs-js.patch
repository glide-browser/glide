diff --git a/browser/components/extensions/parent/ext-tabs.js b/browser/components/extensions/parent/ext-tabs.js
index 0a9b522374bd78dd4038b24e211fa90dd0621b39..2d30eea73ab00f6cb2d6dd88d0558fc869959fe6 100644
--- a/browser/components/extensions/parent/ext-tabs.js
+++ b/browser/components/extensions/parent/ext-tabs.js
@@ -719,7 +719,10 @@ this.tabs = class extends ExtensionAPIPersistent {
           }).then(window => {
             let url;
 
-            let options = { triggeringPrincipal: context.principal };
+            let options = {
+              triggeringPrincipal:
+                context.extension?.loaderPrincipal || context.principal,
+            };
             if (createProperties.cookieStoreId) {
               // May throw if validation fails.
               options.userContextId = getUserContextIdForCookieStoreId(
@@ -880,7 +883,8 @@ this.tabs = class extends ExtensionAPIPersistent {
               flags: updateProperties.loadReplace
                 ? Ci.nsIWebNavigation.LOAD_FLAGS_REPLACE_HISTORY
                 : Ci.nsIWebNavigation.LOAD_FLAGS_NONE,
-              triggeringPrincipal: context.principal,
+              triggeringPrincipal:
+                context.extension?.loaderPrincipal || context.principal,
             };
 
             if (!context.checkLoadURL(url, { dontReportErrors: true })) {
@@ -1661,7 +1665,13 @@ this.tabs = class extends ExtensionAPIPersistent {
               }
             }
           }
-          if (hidden.length) {
+          // Glide's internal extension is the browser itself running the
+          // user's config, so warning that "an extension is hiding tabs"
+          // (and moving the alltabs button around) makes no sense for it.
+          if (
+            hidden.length &&
+            extension.id !== "glide-internal@mozilla.org"
+          ) {
             let win = Services.wm.getMostRecentWindow("navigator:browser");
 
             // Before showing the hidden tabs warning,
